---
title: "Finding & Filtering Items"
description: "Search, filter, sort, and choose columns in the Manage Items module to narrow a cross-project table down to exactly the work you care about."
---

The power of the Manage Items module is in narrowing a large, cross-project list down to exactly the slice you want — then [saving that slice as a view](/manage-items/creating-and-sharing-views). Four tools work together: **search**, **filters**, **sort**, and **columns**.

## Search

The **Search** box at the top of the table matches items by text, so you can jump to a specific item or a set of items by keyword without setting up a filter.

## Filters

Open the **filter** control in the toolbar to narrow the table by the attributes that matter to you. A badge on the control shows how many filters are currently applied, so you always know whether you're looking at everything or a subset.

You can filter by attributes such as **team, status, work type, owner/assignee, labels, priority, start and due dates, board or cycle, blocked state, watchers, and your organization's custom fields** — and combine several at once to zero in on a precise slice (for example, *one team's open, high-priority work due this month*).

<!-- TODO: screenshot — the Manage Items toolbar with the filter panel open and an applied-filter count badge -->

::alert{type="tip" title="The full filter reference"}
Item filtering shares the same engine used across DevStride. For the complete list of filter attributes and how each behaves, see [Filters → Items](/filters/items), and for the matching options, see [Filter Options](/filters/options/items).
::

## Sort

Use the **sort** control to order the table by a column — for example by due date, priority, or status. Like filters, the toolbar shows whether a sort is applied. See [Sort → Items](/filters/sort/items) for details.

## Columns

The table's **column manager** lets you **show, hide, and reorder** the columns the table displays, so you see the fields relevant to your work (and your custom fields) and hide the ones that aren't. Your column choices become part of a [saved view](/manage-items/creating-and-sharing-views), so different views can surface different fields.

<!-- TODO: screenshot — the column manager dialog showing available columns with show/hide toggles -->

## Putting it together

Search, filters, sort, and columns combine into one tailored slice of your work. Once you've built a slice you'll want again — your daily triage list, a client's deliverables, a program roll-up — **save it as a view** so you (and others) can return to it in a click. That's covered next: [Creating & Sharing Views](/manage-items/creating-and-sharing-views).
