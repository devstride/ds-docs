---
title: 2026-08-29
description: "The DevStride Claude Code plugin: plan an architectural roadmap and let it build. Plus Knowledge Base article history with restore, KB tools in the MCP server, and a week of fixes."
links: []
---

## 🚀 Start here — the DevStride Claude Code plugin

**If you write software and you have not tried this yet, this is the part of the release to read twice.**

The plugin turns a DevStride roadmap into an autonomous delivery loop. You plan a real architectural roadmap — solution, capability, epic, story, with the dependencies wired between them — and then the loop walks it for you: selecting the next unblocked item, branching, building, adversarially reviewing its own work, opening the pull request, merging, updating the item, and moving to the next one. Not a code-completion assistant. A planning and delivery loop that runs unattended.

A personal note: our own engineers are seeing well over **20x acceleration** working this way. If you have not picked it up yet, you really should.

📚 [Get the skills](/developer-experience/agentic-skills/getting-the-skills) · [The planning loop](/developer-experience/agentic-skills/claude-skills-planning-loop) · [The delivery loop](/developer-experience/agentic-skills/claude-skills-delivery-loop)

Twelve versions shipped since we last wrote about it. The ones worth knowing:

### 🧠 Your code reviewer can be any capable model (3.2.0)

Every item the loop builds gets a second adversarial review beside Claude's own pass — and that reviewer is now explicitly yours to choose. Grok, Gemini, DeepSeek, and open-weight models served locally all qualify, as does anything behind an OpenAI-compatible endpoint. Setup probes for the engines it knows and always offers to name your own, then validates it against a published contract; doctor holds it to the same contract and never reports an engine as wrong merely for being one it does not recognise. Codex remains what setup offers when you do not choose — a default, not a requirement.

### ⚖️ Review moved to merge boundaries (3.0.0)

Full adversarial review now runs where it pays — at merge boundaries — instead of twice on every story, with routine work getting a bounded risk screen and anything touching authentication, migrations, irreversible state or a deployed contract still verified immediately. Two review cycles is the normal target, with an uncapped continuation for as long as a serious finding stands. This is a major version because one configuration key widened its reach from the local reviewer to every reviewer.

### 📒 Review remembers what it already decided (3.0.0)

Every follow-up reviewer now receives a cumulative ledger of prior findings, verdicts, dispositions and fixes, so a settled finding is not rediscovered or argued back the other way three cycles later. A passing check can only be reused when the tree and the commands genuinely match what produced it.

### 🩺 Doctor fixes things, not just reports them (2.8.0)

`/devstride:doctor` diagnoses read-only exactly as before, then offers the repairs that are safe to automate as one batch, re-verifying each. What it will touch is a fixed classification, not a judgement made in the moment: files inside your repository's `.claude/`, or running the command that already owns a fix. It never touches workflow files, git state, DevStride records, or anything on a deployed stage — and run non-interactively it repairs nothing.

### ⬆️ One command to update (3.1.0)

`/devstride:update` updates the exact installation that loaded it, preserving your install name and scope, verifies the installed files against the published tag rather than trusting an exit code, and stops rather than guessing when an install is pinned or administrator-managed. Better still, turn on auto-update once: `/plugin` → **Marketplaces** → devstride → **Enable auto-update**.

### 🌐 The loop understands deployment stages (2.6.0)

Your configuration can now say which stage a checkout deploys to and which stage names mean production. The release skill names the stage at the go-ahead, so you approve a specific consequence rather than a familiar sentence, and the hotfix skill stops and asks before touching a database on a production stage.

### 🖥️ A status line your whole repository shares (2.6.0)

Setup can write your repository a status line — model, effort, repository, checkout, branch, stage and pull request, each omitted when it does not apply. **Checkout** is the one that earns its place: the loop puts work in worktrees, which is exactly when "which checkout am I in" stops being obvious. Because it lives in the repository rather than one person's settings, everyone gets it.

### 💸 Review got cheaper without getting thinner (2.4.0)

High-risk verification now groups findings by file instead of spawning one agent per finding; a second review round reads only the fixes rather than re-reading the whole diff; and the wait for a cloud reviewer is learned from that reviewer's own history instead of always burning the full timeout.

### 📅 Branch names sort chronologically

Branches the loop creates are `{user}/{YY-MM-DD}/{item}-{slug}`. The year leads so a plain alphabetical listing — `git branch`, tab-completion, the GitHub branch picker — comes out in date order. A month-first date scatters a year's work across the alphabet, which is exactly what this avoids.

Also fixed: status line values could render in a colour many terminal themes place at or near the background, so a value went invisible while its label stayed (3.1.1).

---

## 📚 Knowledge Base article history, with restore

Every Knowledge Base article now keeps a full version history — who changed what, and when — with a **History** panel in the editor. You can preview any earlier version and restore it.

Restoring is deliberately narrow: it affects title and body only. Publish state, slug, visibility, audience and category are left exactly as they are, and nothing is deleted — the restore itself becomes a history entry, so the record of what happened stays complete. A writing session is grouped into a single entry rather than one per keystroke.

## 🔧 Knowledge Base tools in the MCP server

The MCP server gained first-class Knowledge Base tools, so an agent can search, read, write, publish and archive articles — plus a set for reading version history and restoring a version. Anything you can do to an article in the app, you can now do through the MCP server.

## 🔑 MCP sign-in persists between sessions

Authentication to the DevStride MCP server is now stored and reused, so connecting is a one-time step rather than something you repeat every session.

## 🔍 Search finds words behind common prefixes again

Typing a short, common word into search could return nothing at all, because English stop-word processing discarded the term before the prefix match ran. Searching `me` now finds `meetings`. The fix covers both halves of the problem: a query made entirely of common words, and a common word sitting among real ones.

## 🔄 A deploy no longer silently breaks an open tab

When a deploy replaces the front-end bundle, a tab left open from before can request code files that no longer exist and fail in ways that look like random breakage. DevStride now recognises that state and prompts you to reload, replacing an automatic reload that only ever worked in Chromium-based browsers.

## 💬 Notification links land on the actual comment

Following a comment notification used to open the item and leave you to find the comment yourself. The link now takes you to the specific comment it was about.

---

## 🐛 Also fixed

- Comment deep links no longer defeated by a stale comment count
- A comment target is no longer carried onto the next item you open
- Comment notifications no longer fail on system-authored items
- Knowledge Base: choose a category when creating an article
- Knowledge Base: archived articles hidden from listings
- Knowledge Base: edit your own published article
- Knowledge Base: article reordering is less fiddly to drag
- Knowledge Base: proper empty state for an archived-only category
- Training Sandboxes: settings layout, empty states and live updating
- Training Sandboxes: dead sign-ins no longer offered after a teardown
- MCP: explicitly requested fields are no longer trimmed from responses
- MCP: arguments that get stripped are now reported, not silently dropped
- MCP: label names resolve on writes, as the schema documents
- MCP: folder queries honour custom-field and board filters
- MCP: operations taking a request body are reachable again
- MCP: custom fields validated against the work type an item is becoming
- MCP: labels validated against the work type an item is becoming
- MCP: aggregation names the server cannot compute are rejected up front
- MCP: a rejected item type now names the types allowed
- MCP: range errors state both the limit and your value
- MCP: filtering by an exact item number infers its type
- MCP: nested filters can be sent, with user references resolved
- Training Sandboxes: a stale migration record no longer blocks every clone
- Event journal is pruned past its retention horizon
- A stalled journal prune is detected rather than assumed healthy
- Demo dataset: seeded comment threads are visible again
